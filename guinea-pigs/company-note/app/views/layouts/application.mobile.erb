<!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.0 Strict//EN"
    "http://www.w3.org/TR/xhtml1/DTD/xhtml1-strict.dtd">
  <html>
    <head>
      <title><%= h(yield(:title) || "Untitled") %></title>
      <link rel="shortcut icon" href="/assets/favicon.ico" />

      <%= stylesheet_link_tag    "application", :media => "all" %>
      <%= stylesheet_link_tag "jqtouch.min", "theme.min" %>
      <%= javascript_include_tag "application" %>
      <%= javascript_include_tag "jquery.1.3.2.min", "jqtouch", "mobile" %>
      <%= yield(:head) %>
    </head>
    <body>
      <div class="current">
        <%- if show_title? -%>
        <div class="toolbar">
            <%= link_to "Back", root_path, :class => "back" unless current_page? root_path %>
            <h1><%=h yield(:title) %></h1>
            <%= link_to "Full Site", root_url(:mobile => 0), :class => "button", :rel => "external" %>
             <%= yield(:toolbar) %>
          </div>
        <%- end -%>

        <% unless flash.empty? %>
          <div class="info">
          <%- flash.each do |name, msg| -%>
            <%= content_tag :div, msg, :id => "flash_#{name}" %>
          <%- end -%>
          </div>
        <% end %>

        <%= yield %>
        <%= render '/layouts/bottom'%>
      </div>
    </body>
  </html>

